{% trans_default_domain 'auth-telegram.user' %}


{{ form_start(form, { attr: { class: 'd-flex flex-column align-items-end p-4 border shadow rounded-3 w-100' } }) }}

<div class="d-flex flex-column gap-4 w-100 small">

    {# <div class="w-100 d-flex flex-column gap-2">

		</div>

		<div class="w-100 d-flex flex-column gap-2">

		</div>
    #}


    {# {{ 'form.help'|trans({'%url%' : '<a target="_blank" href="'~url~'">'~url~'</a>' })|raw }} #}

    <ol class="small text-muted list-group list-group-numbered list-group-flush">
        <li class="list-group-item px-0">Удерживайте на QR, в меню выбра действий выберите <strong>«Копировать
                изображение»</strong>;
        </li>
        <li class="list-group-item px-0">перейдите в <a target="_blank" href="{{ url }}">{{ url }}</a>, удерживайте на
            поле для текста, и выберите <strong>«Вставить»</strong> и <strong>«Отправить»</strong>;
        </li>
        <li class="list-group-item px-0">введите полученный проверочный код в форму.</li>
    </ol>


    <img data-src='{{ qrcode }}' class="lazy w-100" alt='QR Code'/>

    <div>
        Время действия QR истекает через &nbsp; <span class="auth-telegram-timer fw-bolder small">{{ lifetime }}</span>
    </div>

    <div class="d-flex align-items-end gap-3">

        {{ form_row(form.code, {
            label: 'form.code'|trans
        }) }}

        <div class="mb-3">
            <a href="{{ path('auth-telegram:public.auth') }}" class="btn btn-link text-muted">
                <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" fill="currentColor" viewBox="0 0 16 16">
                    <path fill-rule="evenodd"
                          d="M8 3a5 5 0 1 0 4.546 2.914.5.5 0 0 1 .908-.417A6 6 0 1 1 8 2v1z"></path>
                    <path d="M8 4.466V.534a.25.25 0 0 1 .41-.192l2.36 1.966c.12.1.12.284 0 .384L8.41 4.658A.25.25 0 0 1 8 4.466z"></path>
                </svg>
            </a>
        </div>
    </div>

    <div class="w-100">

        {{ form_row(form.telegram_auth, {
            label: '<span>'~ 'form.save'|trans ~'</span>
				<span class="spinner-border spinner-border-sm vertical-middle d-none"></span>
			',
            attr: { class : 'btn btn-lg btn-primary w-100 rounded-3 text-uppercase fw-bolder' }
        }) }}


    </div>

</div>

{{ form_end(form) }}



{#
{{ form_start(form, { attr: { class: 'shadow d-flex flex-column align-items-end p-5 rounded-3 w-100' } }) }}


<div class="d-flex justify-content-between w-100 mb-4 align-items-end small ps-3">
	<h3 class="text-uppercase h4 fw-bold mb-0 text-nowrap">{{ 'page.index'|trans }}</h3>
</div>

<div class="d-flex flex-column gap-3 w-100 small ps-3">


	<div class="d-flex align-items-end gap-3">

		{{ form_row(form.code, {
			label: 'form.code'|trans
		}) }}

		<div class="mb-3">
			<a href="{{ path('auth-telegram:public.auth') }}" class="btn btn-link text-muted">
				<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" fill="currentColor" viewBox="0 0 16 16">
					<path fill-rule="evenodd"
						  d="M8 3a5 5 0 1 0 4.546 2.914.5.5 0 0 1 .908-.417A6 6 0 1 1 8 2v1z"></path>
					<path d="M8 4.466V.534a.25.25 0 0 1 .41-.192l2.36 1.966c.12.1.12.284 0 .384L8.41 4.658A.25.25 0 0 1 8 4.466z"></path>
				</svg>
			</a>
		</div>
	</div>

	{{ form_row(form.telegram_auth, {
		label: '<span>'~ 'form.save'|trans ~'</span>
				<span class="spinner-border spinner-border-sm vertical-middle d-none"></span>
			',
		attr: { class : 'btn btn-lg btn-primary w-100 rounded-3 text-uppercase fw-bolder' }
	}) }}

</div>

{{ form_end(form) }}
#}



